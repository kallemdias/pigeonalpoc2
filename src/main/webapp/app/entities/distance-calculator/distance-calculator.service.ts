import axios from 'axios';

import buildPaginationQueryOpts from '@/shared/sort/sorts';

import { IDistanceCalculator } from '@/shared/model/distance-calculator.model';

const baseApiUrl = 'api/distance-calculators';

export default class DistanceCalculatorService {
  public find(id: number): Promise<IDistanceCalculator> {
    return new Promise<IDistanceCalculator>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(paginationQuery?: any): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl + `?${buildPaginationQueryOpts(paginationQuery)}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: number): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: IDistanceCalculator): Promise<IDistanceCalculator> {
    return new Promise<IDistanceCalculator>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: IDistanceCalculator): Promise<IDistanceCalculator> {
    return new Promise<IDistanceCalculator>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public partialUpdate(entity: IDistanceCalculator): Promise<IDistanceCalculator> {
    return new Promise<IDistanceCalculator>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity.id}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
